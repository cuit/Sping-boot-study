server:
  port: 8008

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/spring_boot?characterEncoding=utf8
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
study:
  name: zhangsan
  age: 16